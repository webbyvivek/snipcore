# snipcore
Lightweight core foundation plugin for the Snip suite. Provides activation/deactivation lifecycle handling, secure bootstrapping, and a minimal extensible base with no unnecessary dependencies.
